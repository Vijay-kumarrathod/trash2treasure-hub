# Example environment variables for local development / testing
# Replace values with your SMTP provider credentials to enable email notifications

# Mailtrap example (recommended for dev/testing):
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_mailtrap_user
SMTP_PASS=your_mailtrap_pass
SMTP_FROM=no-reply@trash2treasure.local
NOTIFY_EMAILS=you@example.com

# Gmail example (production/dev; use App Passwords for 2FA accounts)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM=your@gmail.com
# NOTIFY_EMAILS=vijay@example.com

# Optional Twilio / SMS env vars (already in server code)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM=
